---
plan: "05-02"
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx
requirements:
  - UI-01
  - UI-02
  - UI-03
autonomous: true
---

# Plan 05-02: Icon Sidebar + Session Drawer

## Objective
Build the crimson icon-only navigation sidebar (`w-20`) and the scrollable session history drawer (`w-72`), as a single `"use client"` component within `AiAdvisorClient.tsx`. These form columns 1 and 2 of the 3-column layout.

## Stitch Reference
- Sidebar: `fixed left-0 top-0 w-20 h-screen bg-[#B31D1D]`, icon buttons with tooltip
- AI Advisor icon active state: `bg-[#FFFFFF]/10 text-[#E8B931]`, `psychology` icon FILL 1
- Drawer: `fixed left-20 top-16 w-72 h-[calc(100vh-64px)] bg-white`
- "Bắt đầu phiên mới" button: `bg-[#B31D1D] text-white rounded-lg`
- Active session item: `bg-[#F7F3EA] text-[#B31D1D] border-l-4 border-[#B31D1D]`
- Inactive session item: `text-slate-600 hover:bg-[#F7F3EA]/50`

## Tasks

<task id="05-02-01">
<title>Create AiAdvisorClient.tsx with full 3-column shell, icon sidebar, and session drawer</title>

<read_first>
- `stitch/.stitch/designs/ai-advisor.html` lines 96-181 — exact sidebar and drawer HTML
- `types/ai-advisor.ts` — ChatSession type
- `.planning/phases/05-pixel-perfect-chat-ui/05-RESEARCH.md` — layout measurements
</read_first>

<action>
Create `app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx` as a complete "use client" component that includes:
1. The 3-column layout shell
2. Icon sidebar (column 1) with Psychology icon active
3. Session history drawer (column 2) with session list
4. Placeholder `<main>` (column 3) — filled by later plans

Implement these nav icons in the sidebar (matching Stitch HTML order):
`dashboard`, `group`, `leaderboard`, `event`, `family_history`, `account_tree`, `database`, `manage_accounts`, `psychology` (active — filled gold)

Session drawer features:
- "Bắt đầu phiên mới" button → clears active session
- Scrollable session list — active item highlighted with left crimson border
- Each item shows `history` icon + truncated title
- Click → set active sessionId

```tsx
"use client";

import Link from "next/link";
import { useState, useCallback } from "react";
import type { ChatSession, ChatMessage } from "@/types/ai-advisor";

interface Session {
  id: string;
  title: string | null;
  created_at: string;
  updated_at: string;
}

interface Props {
  initialSessions: Session[];
}

const NAV_ICONS = [
  { icon: "dashboard", href: "/dashboard", label: "Dashboard" },
  { icon: "group", href: "/dashboard/members", label: "Thành viên" },
  { icon: "leaderboard", href: "/dashboard/stats", label: "Thống kê" },
  { icon: "event", href: "/dashboard/events", label: "Sự kiện" },
  { icon: "family_history", href: "/dashboard/kinship", label: "Quan hệ" },
  { icon: "account_tree", href: "/dashboard/lineage", label: "Phả hệ" },
  { icon: "database", href: "/dashboard/data", label: "Dữ liệu" },
  { icon: "manage_accounts", href: "/dashboard/users", label: "Người dùng" },
];

export default function AiAdvisorClient({ initialSessions }: Props) {
  const [sessions, setSessions] = useState<Session[]>(initialSessions);
  const [activeSessionId, setActiveSessionId] = useState<string | undefined>(undefined);
  const [messages, setMessages] = useState<ChatMessage[]>([]);
  const [isThinking, setIsThinking] = useState(false);
  const [agentSteps, setAgentSteps] = useState<string[]>([]);

  const handleNewSession = useCallback(() => {
    setActiveSessionId(undefined);
    setMessages([]);
    setAgentSteps([]);
  }, []);

  const handleSelectSession = useCallback(async (sessionId: string) => {
    setActiveSessionId(sessionId);
    setAgentSteps([]);

    const res = await fetch(`/api/chat/sessions/${sessionId}`);
    if (res.ok) {
      const { session } = await res.json();
      setMessages(session.messages ?? []);
    }
  }, []);

  const handleDeleteSession = useCallback(async (sessionId: string, e: React.MouseEvent) => {
    e.stopPropagation();
    await fetch(`/api/chat/sessions/${sessionId}`, { method: "DELETE" });
    setSessions((prev) => prev.filter((s) => s.id !== sessionId));
    if (activeSessionId === sessionId) {
      setActiveSessionId(undefined);
      setMessages([]);
    }
  }, [activeSessionId]);

  return (
    <div className="flex h-screen overflow-hidden bg-[#F7F3EA] font-sans">
      {/* ── Column 1: Icon Sidebar ──────────────────────────────── */}
      <aside className="fixed left-0 top-0 h-full z-50 w-20 flex flex-col items-center py-6 border-r border-[#E8B931]/30 bg-[#B31D1D] shadow-lg">
        <div className="text-xl font-bold text-[#E8B931] mb-8" style={{ fontFamily: "'Playfair Display', serif" }}>
          GP
        </div>
        <nav className="flex flex-col gap-4 items-center w-full flex-1 overflow-y-auto">
          {NAV_ICONS.map(({ icon, href, label }) => (
            <Link
              key={icon}
              href={href}
              className="text-[#F7F3EA]/70 p-3 hover:text-white hover:bg-white/5 transition-colors group relative"
              title={label}
            >
              <span className="material-symbols-outlined">{icon}</span>
              <span className="absolute left-full ml-2 px-2 py-1 bg-[#201212] text-white text-[10px] rounded opacity-0 group-hover:opacity-100 transition-opacity whitespace-nowrap z-50">
                {label}
              </span>
            </Link>
          ))}
          {/* AI Advisor — active */}
          <button
            className="bg-white/10 text-[#E8B931] rounded-lg p-3 transition-all group relative"
            aria-label="AI Advisor"
          >
            <span
              className="material-symbols-outlined"
              style={{ fontVariationSettings: "'FILL' 1" }}
            >
              psychology
            </span>
            <span className="absolute left-full ml-2 px-2 py-1 bg-[#201212] text-white text-[10px] rounded opacity-0 group-hover:opacity-100 transition-opacity whitespace-nowrap z-50">
              AI Advisor
            </span>
          </button>
        </nav>
      </aside>

      {/* ── Column 2: Session Drawer ───────────────────────────── */}
      <aside className="fixed left-20 top-0 h-screen z-40 w-72 border-r border-[#F1EDE2] bg-white flex flex-col shadow-none pt-4 px-4">
        <div className="flex flex-col mb-6">
          <h2 className="text-[#B31D1D] text-[1.125rem] font-bold" style={{ fontFamily: "'Playfair Display', serif" }}>
            Lịch sử hỏi đáp
          </h2>
          <p className="text-slate-500 text-[0.75rem] font-medium uppercase tracking-widest">
            Session History
          </p>
        </div>

        <button
          onClick={handleNewSession}
          className="w-full bg-[#B31D1D] text-white py-3 px-4 rounded-lg flex items-center justify-center gap-2 mb-6 hover:bg-[#93000C] transition-all shadow-md"
        >
          <span className="material-symbols-outlined text-sm">add</span>
          <span className="font-medium text-sm">Bắt đầu phiên mới</span>
        </button>

        <div className="flex-1 overflow-y-auto space-y-1">
          {sessions.length === 0 && (
            <p className="text-slate-400 text-xs text-center py-8">Chưa có phiên hỏi đáp</p>
          )}
          {sessions.map((session) => {
            const isActive = session.id === activeSessionId;
            return (
              <div
                key={session.id}
                onClick={() => handleSelectSession(session.id)}
                className={`group p-3 flex items-center gap-3 rounded-r-md cursor-pointer transition-all ${
                  isActive
                    ? "bg-[#F7F3EA] text-[#B31D1D] font-semibold border-l-4 border-[#B31D1D] translate-x-1"
                    : "text-slate-600 hover:bg-[#F7F3EA]/50 border-l-4 border-transparent"
                }`}
              >
                <span className="material-symbols-outlined text-[18px] shrink-0">history</span>
                <span className="text-sm truncate flex-1">
                  {session.title ?? "Phiên mới"}
                </span>
                <button
                  onClick={(e) => handleDeleteSession(session.id, e)}
                  className="opacity-0 group-hover:opacity-100 text-slate-400 hover:text-red-500 transition-all"
                  title="Xoá phiên"
                >
                  <span className="material-symbols-outlined text-[16px]">delete</span>
                </button>
              </div>
            );
          })}
        </div>

        <div className="bg-[#F1EDE2] h-[1px] my-4" />
        <div className="p-2 pb-4">
          <p className="text-[0.65rem] text-slate-400 uppercase tracking-tighter">
            Powered by Heritage Crimson Intelligence
          </p>
        </div>
      </aside>

      {/* ── Column 3: Main content (filled by parent) ─────────── */}
      <main className="ml-[368px] flex-1 flex flex-col min-w-0 h-screen relative overflow-hidden">
        {/* Top app bar, chat canvas, input — added in Plan 05-03/04/05 */}
        <div className="flex-1 flex items-center justify-center text-slate-400">
          <p>Chat canvas loading...</p>
        </div>
      </main>
    </div>
  );
}
```

Note: The `<main>` placeholder will be replaced in Plans 05-03 through 05-05.
</action>

<acceptance_criteria>
- `app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx` exists
- File starts with `"use client";`
- Contains `<aside>` for icon sidebar with `bg-[#B31D1D]`, `w-20`
- Contains psychology icon with `FILL 1` style (active icon)
- Contains session drawer with `w-72`, `left-20`
- "Bắt đầu phiên mới" button calls `handleNewSession`
- Session items highlight correctly (crimson border-l-4 when active)
- Delete button on hover for each session
- `npx tsc --noEmit` exits 0
</acceptance_criteria>
</task>

## Verification

```bash
grep "use client" "app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx"
grep "bg-\[#B31D1D\]" "app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx"
grep "w-20" "app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx"
grep "psychology" "app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx"
grep "FILL.*1" "app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx"
grep "handleNewSession\|handleSelectSession\|handleDeleteSession" "app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx"
npx tsc --noEmit 2>&1 | head -20
```

## Must-Haves
- [ ] Icon sidebar with all 9 nav items + active psychology icon gold
- [ ] Session drawer with "Bắt đầu phiên mới" + scrollable session list
- [ ] Active session highlighted (left crimson border)
- [ ] Delete icon on hover per session item
