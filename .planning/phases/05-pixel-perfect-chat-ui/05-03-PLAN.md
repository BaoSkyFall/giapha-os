---
plan: "05-03"
wave: 3
depends_on: ["05-02"]
files_modified:
  - app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx
requirements:
  - UI-05
  - UI-06
  - UI-07
  - UI-08
  - UI-09
  - UI-10
  - UI-11
  - UI-19
  - UI-20
autonomous: true
---

# Plan 05-03: Top App Bar + Chat Canvas (Messages)

## Objective
Replace the `<main>` placeholder in `AiAdvisorClient.tsx` with the actual top app bar and chat canvas. Implement user message bubbles and AI response cards matching the Stitch design exactly.

## Stitch Reference
- App bar: `h-16`, "Trợ lý AI Tộc Phạm Phú" in Playfair Display italic bold crimson, green pulse indicator
- Chat canvas: `bg-[#F7F3EA]` with `.paper-texture` + lotus SVG watermark bottom-right
- User message: `max-w-[70%] bg-white shadow-md rounded-2xl rounded-tr-none border-r-4 border-[#E8B931]`
- AI message: `max-w-[80%] bg-white shadow-xl rounded-2xl rounded-tl-none border-t-4 border-[#B31D1D]`
- AI avatar: `w-10 h-10 rounded-full bg-[#B31D1D]` with `temple_buddhist` icon
- AI card accent: `h-1 bg-gradient-to-r from-[#E8B931] via-[#B31D1D] to-[#E8B931] opacity-50`
- List bullets: gold dot `w-2 h-2 rounded-full bg-[#E8B931]`
- All messages: `animate-in fade-in slide-in-from-bottom-4`

## Tasks

<task id="05-03-01">
<title>Add top app bar and chat canvas to AiAdvisorClient.tsx</title>

<read_first>
- `stitch/.stitch/designs/ai-advisor.html` lines 183-296 — exact TopAppBar and chat canvas HTML
- `app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx` — current state (replace `<main>` placeholder)
- `types/ai-advisor.ts` — ChatMessage type (role, content, subject_id, metadata)
</read_first>

<action>
Replace the placeholder `<main>` in `AiAdvisorClient.tsx` with the full implementation:

### Top App Bar
```tsx
<header className="flex justify-between items-center px-8 w-full h-16 border-b-2 border-[#E8B931]/20 bg-[#F7F3EA] shadow-sm sticky top-0 z-30">
  <div className="flex items-center gap-4">
    <span className="material-symbols-outlined text-[#B31D1D]"
          style={{ fontVariationSettings: "'FILL' 1" }}>psychology</span>
    <div>
      <h1 className="text-[#B31D1D] italic text-xl font-bold"
          style={{ fontFamily: "'Playfair Display', serif" }}>
        Trợ lý AI Tộc Phạm Phú
      </h1>
      <div className="flex items-center gap-1.5">
        <span className="w-2 h-2 rounded-full bg-emerald-500 animate-pulse" />
        <span className="text-[0.65rem] font-bold text-emerald-600 uppercase tracking-widest">
          Đang hoạt động
        </span>
      </div>
    </div>
  </div>
  <div className="flex items-center gap-6">
    <div className="hidden md:flex items-center bg-[#F1EDE2] rounded-full px-4 py-1.5 border border-[#E8B931]/20">
      <span className="material-symbols-outlined text-sm text-slate-500 mr-2">search</span>
      <input className="bg-transparent border-none focus:ring-0 text-sm w-48 text-slate-700 outline-none"
             placeholder="Tìm kiếm trong hội thoại..." type="text" />
    </div>
    <div className="flex items-center gap-3 text-slate-600">
      <button className="hover:text-[#B31D1D] transition-colors opacity-80 hover:opacity-100">
        <span className="material-symbols-outlined">history</span>
      </button>
      <button className="hover:text-[#B31D1D] transition-colors opacity-80 hover:opacity-100">
        <span className="material-symbols-outlined">settings</span>
      </button>
      <button className="hover:text-[#B31D1D] transition-colors opacity-80 hover:opacity-100">
        <span className="material-symbols-outlined">help</span>
      </button>
    </div>
  </div>
</header>
```

### Chat Canvas
```tsx
<section className="flex-1 overflow-y-auto p-8 flex flex-col gap-8 relative bg-[#F7F3EA]" ref={chatEndRef}>
  {/* Lotus watermark */}
  <svg className="absolute bottom-[-50px] right-[-50px] opacity-5 w-[400px] pointer-events-none"
       fill="currentColor" viewBox="0 0 100 100">
    <path d="M50 0C53 20 65 30 80 35C65 40 53 50 50 70C47 50 35 40 20 35C35 30 47 20 50 0Z" fill="#B31D1D"/>
    <path d="M50 30C52 40 60 45 70 47C60 50 52 55 50 65C48 55 40 50 30 47C40 45 48 40 50 30Z" fill="#E8B931"/>
  </svg>

  {/* Messages */}
  {messages.map((msg, i) => msg.role === "user" ? (
    <UserMessage key={i} content={msg.content} />
  ) : (
    <AiMessage key={i} content={msg.content} subjectId={msg.subject_id} />
  ))}

  {/* Thinking state — shown while isThinking */}
  {isThinking && <ThinkingState steps={agentSteps} />}

  <div ref={messagesEndRef} />
</section>
```

### UserMessage sub-component
```tsx
function UserMessage({ content }: { content: string }) {
  return (
    <div className="flex justify-end animate-in fade-in slide-in-from-bottom-4 duration-500">
      <div className="max-w-[70%] bg-white shadow-md rounded-2xl rounded-tr-none p-4 border-r-4 border-[#E8B931]">
        <p className="text-[#201212] leading-relaxed">{content}</p>
      </div>
    </div>
  );
}
```

### AiMessage sub-component
```tsx
function AiMessage({ content, subjectId }: { content: string; subjectId?: string }) {
  // Render simple paragraphs + bullet lists
  const lines = content.split("\n");
  return (
    <div className="flex justify-start animate-in fade-in slide-in-from-bottom-4 duration-700 delay-200">
      <div className="max-w-[80%] flex gap-4">
        <div className="flex-shrink-0">
          <div className="w-10 h-10 rounded-full bg-[#B31D1D] flex items-center justify-center shadow-lg border-2 border-[#E8B931]/30">
            <span className="material-symbols-outlined text-white text-xl">temple_buddhist</span>
          </div>
        </div>
        <div className="bg-white shadow-xl rounded-2xl rounded-tl-none overflow-hidden border-t-4 border-[#B31D1D]">
          <div className="h-1 bg-gradient-to-r from-[#E8B931] via-[#B31D1D] to-[#E8B931] opacity-50" />
          <div className="p-5">
            <div className="text-[#201212] leading-relaxed space-y-2">
              {lines.map((line, i) =>
                line.startsWith("- ") ? (
                  <li key={i} className="flex items-start gap-3 list-none">
                    <span className="w-5 h-5 rounded-full bg-[#E8B931]/20 flex items-center justify-center mt-0.5 shrink-0">
                      <span className="w-2 h-2 rounded-full bg-[#E8B931]" />
                    </span>
                    <span dangerouslySetInnerHTML={{ __html: line.slice(2).replace(/\*\*(.*?)\*\*/g, "<strong>$1</strong>") }} />
                  </li>
                ) : (
                  <p key={i} dangerouslySetInnerHTML={{ __html: line.replace(/\*\*(.*?)\*\*/g, "<strong>$1</strong>") }} />
                )
              )}
            </div>
            {subjectId && (
              <a
                href={`/dashboard/members/${subjectId}`}
                className="inline-flex items-center gap-2 text-[#B31D1D] font-bold text-sm mt-4 hover:underline decoration-[#E8B931] decoration-2 underline-offset-4"
              >
                Xem trong cây gia phả
                <span className="material-symbols-outlined text-sm">arrow_forward</span>
              </a>
            )}
          </div>
        </div>
      </div>
    </div>
  );
}
```

Also add two refs:
```ts
const chatEndRef = useRef<HTMLDivElement>(null);
const messagesEndRef = useRef<HTMLDivElement>(null);
```
And auto-scroll effect:
```ts
useEffect(() => {
  messagesEndRef.current?.scrollIntoView({ behavior: "smooth" });
}, [messages, isThinking]);
```
</action>

<acceptance_criteria>
- `AiAdvisorClient.tsx` contains `<header>` with `h-16`, "Trợ lý AI Tộc Phạm Phú" in Playfair Display italic
- Green pulse dot: `w-2 h-2 rounded-full bg-emerald-500 animate-pulse`
- Chat canvas has lotus SVG watermark with `opacity-5`
- `UserMessage` has `rounded-tr-none border-r-4 border-[#E8B931]`
- `AiMessage` has `rounded-tl-none border-t-4 border-[#B31D1D]` and `temple_buddhist` icon
- AI card has gold-to-crimson gradient accent bar
- "Xem trong cây gia phả" link renders when `subjectId` is present
- Gold dot bullets for list items
- Auto-scroll to bottom on new messages
- `npx tsc --noEmit` exits 0
</acceptance_criteria>
</task>

## Verification

```bash
grep "Trợ lý AI Tộc Phạm Phú" "app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx"
grep "animate-pulse" "app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx"
grep "temple_buddhist" "app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx"
grep "lotus" "app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx"
grep "Xem trong cây gia phả" "app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx"
grep "border-r-4 border-\[#E8B931\]" "app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx"
npx tsc --noEmit 2>&1 | head -20
```

## Must-Haves
- [ ] Top bar has Playfair Display italic crimson title + green pulse
- [ ] User messages: right-aligned, white, rounded-tr-none, gold right border
- [ ] AI messages: left-aligned, crimson top border, temple_buddhist avatar, gradient bar
- [ ] "Xem trong cây gia phả" link appears when subject_id is set
- [ ] Auto-scroll to bottom on new messages
