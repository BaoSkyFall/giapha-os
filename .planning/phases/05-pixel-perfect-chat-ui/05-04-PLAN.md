---
plan: "05-04"
wave: 3
depends_on: ["05-02"]
files_modified:
  - app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx
requirements:
  - UI-12
  - UI-13
  - UI-14
  - UI-15
  - UI-16
  - UI-17
autonomous: true
---

# Plan 05-04: Thinking State + Input Footer

## Objective
Add the thinking state (3-dot bounce + agent checklist) and the full input footer (suggestion chips + textarea + send button + disclaimer) to AiAdvisorClient.tsx.

## Stitch Reference
- Thinking: 3 crimson dots `animate-bounce` with staggered delays, bordered-left agent checklist
- Completed step: `check_circle` emerald icon + `text-slate-400 uppercase`
- Active step: spinning border circle + `text-[#B31D1D] uppercase`
- Chips: `rounded-full bg-white border border-[#E8B931]/30 hover:border-[#B31D1D] hover:text-[#B31D1D]`
- Input: `pl-12 pr-16 py-4 rounded-2xl border-2 focus:border-[#B31D1D]`, `auto_awesome` icon left
- Send button: `bg-[#B31D1D] text-white rounded-xl`, `send` icon
- Disclaimer: `text-[10px] text-slate-400 tracking-wider` centered

## Tasks

<task id="05-04-01">
<title>Add ThinkingState component, ChatInput with chips, to AiAdvisorClient.tsx</title>

<read_first>
- `stitch/.stitch/designs/ai-advisor.html` lines 297-330 — ThinkingState HTML
- `stitch/.stitch/designs/ai-advisor.html` lines 332-359 — Input footer HTML
- `types/ai-advisor.ts` — AgentStep type + NDJSON StreamChunk types
</read_first>

<action>

### ThinkingState sub-component
```tsx
function ThinkingState({ steps }: { steps: string[] }) {
  const STEP_LABELS: Record<string, string> = {
    parsing: "Phân tích câu hỏi",
    searching: "Truy vấn cơ sở dữ liệu",
    verifying: "Xác minh danh tính",
    narrating: "Tổng hợp hồ sơ thành viên...",
  };

  const ALL_STEPS = ["parsing", "searching", "verifying", "narrating"];

  return (
    <div className="flex justify-start animate-in fade-in duration-500">
      <div className="max-w-[80%] flex gap-4">
        <div className="flex-shrink-0">
          <div className="w-10 h-10 rounded-full bg-[#B31D1D]/20 flex items-center justify-center border-2 border-[#B31D1D]/10">
            <span className="material-symbols-outlined text-[#B31D1D] text-xl animate-pulse">psychology</span>
          </div>
        </div>
        <div className="bg-[#F1EDE2]/50 border border-[#E8B931]/10 p-4 rounded-2xl rounded-tl-none">
          <div className="flex items-center gap-3 text-slate-500 mb-2">
            <div className="flex gap-1">
              <span className="w-1.5 h-1.5 bg-[#B31D1D] rounded-full animate-bounce [animation-delay:-0.3s]" />
              <span className="w-1.5 h-1.5 bg-[#B31D1D] rounded-full animate-bounce [animation-delay:-0.15s]" />
              <span className="w-1.5 h-1.5 bg-[#B31D1D] rounded-full animate-bounce" />
            </div>
            <span className="text-xs font-medium italic">Đang tìm kiếm trong gia phả...</span>
          </div>
          <div className="flex flex-col gap-1.5 border-l border-[#B31D1D]/30 pl-3">
            {ALL_STEPS.map((step, i) => {
              const done = steps.includes(step) && steps[steps.length - 1] !== step;
              const active = steps[steps.length - 1] === step;
              const pending = !steps.includes(step);
              if (pending) return null;
              return (
                <div key={step} className="flex items-center gap-2">
                  {done ? (
                    <span className="material-symbols-outlined text-[14px] text-emerald-600">check_circle</span>
                  ) : active ? (
                    <span className="w-3 h-3 border-2 border-[#B31D1D] border-t-transparent rounded-full animate-spin" />
                  ) : null}
                  <span className={`text-[10px] uppercase tracking-widest font-bold ${
                    done ? "text-slate-400" : "text-[#B31D1D]"
                  }`}>
                    {STEP_LABELS[step] ?? step}
                  </span>
                </div>
              );
            })}
          </div>
        </div>
      </div>
    </div>
  );
}
```

### ChatInput component (replace footer placeholder)
```tsx
const SUGGESTION_CHIPS = [
  "Tổ tiên đời đầu tiên là ai?",
  "Ông Phạm Phú Ba có mấy người con?",
  "Lịch giỗ họ tháng này",
];

function ChatInput({
  onSend,
  disabled,
}: {
  onSend: (msg: string) => void;
  disabled: boolean;
}) {
  const [value, setValue] = useState("");

  const handleSend = () => {
    if (!value.trim() || disabled) return;
    onSend(value.trim());
    setValue("");
  };

  const handleKey = (e: React.KeyboardEvent) => {
    if (e.key === "Enter" && !e.shiftKey) {
      e.preventDefault();
      handleSend();
    }
  };

  return (
    <footer className="p-6 bg-[#FFFCF5] border-t border-[#E8B931]/20">
      <div className="max-w-4xl mx-auto flex flex-col gap-4">
        {/* Suggestion chips */}
        <div className="flex flex-wrap gap-2 mb-2">
          {SUGGESTION_CHIPS.map((chip) => (
            <button
              key={chip}
              onClick={() => onSend(chip)}
              disabled={disabled}
              className="px-4 py-1.5 rounded-full bg-white border border-[#E8B931]/30 text-xs font-medium text-slate-600 hover:border-[#B31D1D] hover:text-[#B31D1D] transition-all shadow-sm disabled:opacity-50"
            >
              {chip}
            </button>
          ))}
        </div>
        {/* Input row */}
        <div className="relative flex items-center group">
          <div className="absolute left-4 flex items-center text-[#B31D1D]/40 group-focus-within:text-[#B31D1D] transition-colors">
            <span className="material-symbols-outlined">auto_awesome</span>
          </div>
          <input
            type="text"
            value={value}
            onChange={(e) => setValue(e.target.value)}
            onKeyDown={handleKey}
            disabled={disabled}
            placeholder="Hỏi về bất kỳ thành viên, sự kiện hoặc lịch sử tộc Phạm Phú..."
            className="w-full pl-12 pr-16 py-4 bg-white rounded-2xl border-2 border-[#E8B931]/20 focus:border-[#B31D1D] focus:outline-none shadow-lg transition-all text-[#201212] disabled:opacity-60"
          />
          <button
            onClick={handleSend}
            disabled={disabled || !value.trim()}
            className="absolute right-3 p-2 bg-[#B31D1D] text-white rounded-xl shadow-md hover:bg-[#93000C] active:scale-95 transition-all disabled:opacity-50"
            aria-label="Gửi"
          >
            <span className="material-symbols-outlined">send</span>
          </button>
        </div>
        {/* Disclaimer */}
        <p className="text-center text-[10px] text-slate-400 font-medium tracking-wider">
          THÔNG TIN AI CUNG CẤP CÓ THỂ CẦN ĐƯỢC XÁC MINH VỚI TRƯỞNG TỘC
        </p>
      </div>
    </footer>
  );
}
```

Add `<ChatInput onSend={handleSend} disabled={isThinking} />` as the last child of `<main>` after the chat canvas section.
</action>

<acceptance_criteria>
- `ThinkingState` component exists in `AiAdvisorClient.tsx`
- 3 bouncing dots with staggered `animation-delay` values
- Agent steps render as: done → `check_circle` emerald; active → spinner
- `ChatInput` exists with `auto_awesome` icon, `send` button, 3 suggestion chips
- Send button calls `onSend` on click or Enter key
- Disclaimer text exactly: "THÔNG TIN AI CUNG CẤP CÓ THỂ CẦN ĐƯỢC XÁC MINH VỚI TRƯỞNG TỘC"
- Input disabled while `isThinking === true`
- `npx tsc --noEmit` exits 0
</acceptance_criteria>
</task>

## Verification

```bash
grep "ThinkingState" "app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx"
grep "animation-delay" "app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx"
grep "check_circle" "app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx"
grep "auto_awesome" "app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx"
grep "THÔNG TIN AI" "app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx"
grep "ChatInput" "app/(ai-advisor)/dashboard/ai-advisor/AiAdvisorClient.tsx"
npx tsc --noEmit 2>&1 | head -20
```

## Must-Haves
- [ ] 3-dot bounce animation with staggered delays
- [ ] Agent checklist with per-step icons (done/active)
- [ ] Input with auto_awesome icon + crimson focus border
- [ ] Send button is crimson, disabled while thinking
- [ ] 3 suggestion chips with hover:border-primary effect
- [ ] Footer disclaimer text exact match
