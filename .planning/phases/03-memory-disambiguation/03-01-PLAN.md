---
plan: "03-01"
wave: 1
depends_on: []
files_modified:
  - types/ai-advisor.ts
requirements:
  - AGT-04
  - AGT-06
  - MEM-02
  - MEM-04
autonomous: true
---

# Plan 03-01: Extend Types for Disambiguation

## Objective
Extend `types/ai-advisor.ts` with 2 additions needed for Phase 3: a `clarify` variant in `StreamChunk` and extended fields in `ChatScratchpad`. These types gate all Agent 4 and route.ts work in waves 2–3.

## Tasks

<task id="03-01-01">
<title>Add 'clarify' to StreamChunk union and extend ChatScratchpad</title>

<read_first>
- `types/ai-advisor.ts` — current `StreamChunk` union (4 variants) and `ChatScratchpad` interface
- `.planning/phases/03-memory-disambiguation/03-CONTEXT.md` — exact field names required
</read_first>

<action>
In `types/ai-advisor.ts`, make the following two targeted changes:

**1. Add `clarify` variant to `StreamChunk` union** — insert after the existing `done` variant:
```ts
| { type: 'clarify'; question: string; candidates: PersonSearchResult[] }
```

The full updated `StreamChunk` type should be:
```ts
export type StreamChunk =
  | { type: "agent_step"; step: AgentStep; label: string }
  | { type: "text"; delta: string }
  | { type: "done"; sessionId: string; subject?: PersonSummary }
  | { type: "clarify"; question: string; candidates: PersonSearchResult[] }
  | { type: "error"; message: string; code?: string };
```

**2. Extend `ChatScratchpad` interface** — add 3 new optional fields:
```ts
export interface ChatScratchpad {
  confirmed_subject_id?: string;
  candidates?: PersonSearchResult[];        // saved candidates for follow-up re-search
  pending_clarification?: boolean;          // true = last turn ended with clarify chunk
  clarification_round?: number;             // 0-indexed, max MAX_CLARIFICATION_ROUNDS (2)
}
```
</action>

<acceptance_criteria>
- `types/ai-advisor.ts` has `StreamChunk` with exactly 5 variants (agent_step, text, done, clarify, error)
- `StreamChunk` contains `| { type: "clarify"; question: string; candidates: PersonSearchResult[] }`
- `ChatScratchpad` contains `pending_clarification?: boolean`
- `ChatScratchpad` contains `clarification_round?: number`
- `ChatScratchpad` contains `candidates?: PersonSearchResult[]`
- `npx tsc --noEmit` exits 0 after changes
</acceptance_criteria>
</task>

## Verification

```bash
grep "clarify" types/ai-advisor.ts
grep "pending_clarification" types/ai-advisor.ts
grep "clarification_round" types/ai-advisor.ts
npx tsc --noEmit 2>&1 | head -10
```

## Must-Haves
- [ ] `StreamChunk` has `clarify` variant with `question` and `candidates` fields
- [ ] `ChatScratchpad` has 3 new optional fields
- [ ] No TypeScript errors
